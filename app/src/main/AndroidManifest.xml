<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.INTERNET"/>

  <application
      android:name=".application.ShowScrollApplication"
      android:allowBackup="true"
      android:dataExtractionRules="@xml/data_extraction_rules"
      android:fullBackupContent="@xml/backup_rules"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.ShowScroll"
      tools:targetApi="31">
    <activity
        android:name=".features.home.presentation.ShowActivity"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name="com.example.showscroll.features.showdetail.presentation.ShowDetailActivity"
        android:label=""
        android:parentActivityName=".features.showdetail.presentation.ShowDetailActivity"/>
    <activity android:name="com.example.showscroll.features.seasons.presentation.SeasonActivity"
        android:label=""
        />
    <activity android:name="com.example.showscroll.features.episodedetail.presentation.EpisodesDetail"
        android:label=""
        />
  </application>

</manifest>